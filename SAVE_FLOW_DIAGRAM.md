# 💾 Save Flow Diagram - Calculator System V2

## 🎯 Golden Rule
**ONE SAVE BUTTON = ONE ATOMIC TRANSACTION**

No field saves, no formula saves, no partial updates. Everything or nothing.

---

## 📊 Flow Diagram (Text)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          USER INTERACTION                                │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  User edits:                                                             │
│  - Calculator name → State: draftCalculator.name                        │
│  - Adds field      → State: draftCalculator.fields.push(newField)      │
│  - Edits formula   → State: draftCalculator.formulas[i].expression     │
│  - Deletes field   → State: draftCalculator.fields.filter(...)         │
│                                                                          │
│  ALL CHANGES ARE LOCAL ONLY (Not sent to server)                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │
                          User clicks "Save"
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        FRONTEND VALIDATION                               │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
            ┌───────────────┐            ┌──────────────────┐
            │  Valid?       │            │  Invalid?        │
            │  ✅           │            │  ❌              │
            └───────┬───────┘            └────────┬─────────┘
                    │                             │
                    │                             ▼
                    │                    Show validation errors
                    │                    STOP - Don't send request
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    FRONTEND PAYLOAD CONSTRUCTION                         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │
        Build single payload:       │
        {                           │
          name: "...",              │
          description: "...",       │
          calcCategory: "...",      │
          fields: [                 │
            { fieldName: "...", ... },
            { fieldName: "...", ... }
          ],                        │
          formulas: [               │
            { formulaName: "...", formulaExpression: "..." }
          ]                         │
        }                           │
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      API REQUEST (Single Call)                          │
│  PUT /api/v1/calculators/{id}                                           │
│  Body: { ...calculator, fields: [...], formulas: [...] }               │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    BACKEND CONTROLLER (NestJS)                           │
│  @Put(':id')                                                            │
│  async update(@Param('id') id, @Body() dto: UpdateCalculatorDto)       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       DTO VALIDATION (class-validator)                  │
│  - Check required fields                                                │
│  - Validate field types                                                 │
│  - Check enum values                                                    │
│  - Validate nested objects                                              │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
            ┌───────────────┐            ┌──────────────────┐
            │  Valid?       │            │  Invalid?        │
            │  ✅           │            │  ❌              │
            └───────┬───────┘            └────────┬─────────┘
                    │                             │
                    │                             ▼
                    │                    Return 400 Bad Request
                    │                    Frontend shows error
                    │                    ROLLBACK - No changes saved
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    BACKEND SERVICE (Atomic Transaction)                 │
│  async update(id, dto, userId, accessToken)                            │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  STEP 1: Update calculator metadata                                     │
│    UPDATE calculators SET name = ?, description = ? WHERE id = ?       │
│    ✅ Success → Continue                                                │
│    ❌ Fail    → Throw error, STOP                                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  STEP 2: Replace ALL fields (if provided)                              │
│    DELETE FROM calculator_fields WHERE calculator_id = ?               │
│    INSERT INTO calculator_fields (calculator_id, ...) VALUES ...       │
│    ✅ Success → Continue                                                │
│    ❌ Fail    → Rollback Step 1, STOP                                   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  STEP 3: Replace ALL formulas (if provided)                            │
│    DELETE FROM calculator_formulas WHERE calculator_id = ?             │
│    INSERT INTO calculator_formulas (calculator_id, ...) VALUES ...     │
│    ✅ Success → COMMIT TRANSACTION                                      │
│    ❌ Fail    → Rollback Steps 1 & 2, STOP                              │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    DATABASE TRANSACTION COMMITS                          │
│  All changes are persisted atomically                                   │
│  PostgreSQL ACID guarantees apply                                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    BACKEND RETURNS RESPONSE                              │
│  {                                                                       │
│    id: "...",                                                           │
│    name: "Updated Calculator",                                          │
│    fields: [ { id: "...", fieldName: "..." } ],                        │
│    formulas: [ { id: "...", formulaName: "..." } ],                    │
│    version: 2,  ← Incremented                                          │
│    updatedAt: "2026-01-04T10:00:00Z"                                   │
│  }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    FRONTEND RECEIVES RESPONSE                            │
│  React Query mutation onSuccess()                                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  OPTIMISTIC UPDATE (Optional)                                           │
│  queryClient.setQueryData(['calculator', id], responseData)            │
│  UI updates immediately with server response                            │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  REFETCH FROM SERVER (Confirmation)                                     │
│  queryClient.invalidateQueries(['calculator', id])                     │
│  Ensures UI matches database exactly                                    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  CLEAR DRAFT STATE                                                      │
│  setDraftCalculator(null)                                              │
│  User sees "saved" indicator                                            │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                            ┌───────────────┐
                            │   SUCCESS ✅  │
                            └───────────────┘
```

---

## 🚨 Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    ERROR OCCURS (Any Step)                              │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  BACKEND CATCHES ERROR                                                  │
│  - Logs error                                                           │
│  - Rolls back any partial changes                                       │
│  - Returns error response                                               │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  FRONTEND MUTATION onError()                                            │
│  - Rollback optimistic update (if any)                                  │
│  - Restore previous state                                               │
│  - Show error toast                                                     │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  USER SEES ERROR MESSAGE                                                │
│  - "Failed to save calculator: [reason]"                                │
│  - Draft state preserved (can retry)                                    │
│  - No partial data in database ✅                                       │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Properties of This Flow

### 1. **Atomicity**
- ✅ All changes commit together
- ✅ No partial saves possible
- ✅ Database always in consistent state

### 2. **Single Source of Truth**
- ✅ Database is authoritative
- ✅ Frontend refetches after save
- ✅ No stale local state

### 3. **Error Recovery**
- ✅ Rollback on any failure
- ✅ User can retry
- ✅ No data corruption

### 4. **User Experience**
- ✅ Optimistic updates (feels fast)
- ✅ Confirmation refetch (accurate)
- ✅ Clear "unsaved changes" indicator

---

## 📋 Comparison: Old vs New

| Aspect | ❌ Old System | ✅ New System |
|--------|--------------|--------------|
| **Save Actions** | 3 separate calls | 1 atomic call |
| **State Management** | Split across components | Unified in parent |
| **Error Handling** | Partial failures possible | All-or-nothing |
| **Data Consistency** | Stale state risks | Always fresh from DB |
| **Type Safety** | `any` everywhere | Strict types |
| **Testing** | Hard to test | Easy to test (single flow) |

---

## 🛡️ Safety Guarantees

1. **Database Level**
   - Foreign key constraints prevent orphans
   - Transactions ensure atomicity
   - RLS prevents unauthorized access

2. **Backend Level**
   - DTO validation catches bad data
   - Service layer has try-catch
   - Rollback on any error

3. **Frontend Level**
   - TypeScript catches type errors
   - React Query handles cache
   - Optimistic updates have rollback

---

## 🔐 Security Notes

- ✅ RLS policies enforce user ownership
- ✅ No data leakage between users
- ✅ Auth token validated on every request
- ✅ Input validation at multiple layers

---

## 📈 Performance Notes

- ✅ Single request (not 3+) reduces latency
- ✅ Optimistic updates make UI feel instant
- ✅ Indexes on foreign keys for fast lookups
- ✅ Only refetch changed data (React Query smart)

---

## 🎓 Teaching Moment

**Why is this better?**

Old system:
```typescript
// ❌ 3 requests = 3 failure points
await saveCalculator(metadata)  // ✅ Success
await saveFields(fields)        // ❌ FAILS - partial state!
await saveFormulas(formulas)    // ⏸️ Never runs
```

New system:
```typescript
// ✅ 1 request = 1 failure point
await saveCalculator({
  ...metadata,
  fields,
  formulas
})
// Either ALL succeed or ALL fail
```
